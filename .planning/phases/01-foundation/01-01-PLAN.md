---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tsconfig.app.json
  - src/main.tsx
  - src/App.tsx
  - src/styles/globals.css
  - src/vite-env.d.ts
  - .env.example
  - .env.local
  - .gitignore
  - index.html
autonomous: true

must_haves:
  truths:
    - "npm run dev starts development server without errors"
    - "Browser shows React app at localhost:5173"
    - "TypeScript compilation succeeds with strict mode"
    - "Tailwind CSS utility classes work in components"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "react@^19"
    - path: "vite.config.ts"
      provides: "Vite configuration with Tailwind"
      contains: "tailwindcss"
    - path: "src/styles/globals.css"
      provides: "Tailwind CSS entry point"
      contains: "@import"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "VITE_SUPABASE_URL"
  key_links:
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "plugin import"
      pattern: "import tailwindcss"
    - from: "src/main.tsx"
      to: "src/styles/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Create Vite + React 19 + TypeScript project with Tailwind CSS v4 and environment configuration.

Purpose: Establish the frontend project foundation with modern tooling and styling infrastructure.
Output: Working React development environment with Tailwind CSS ready for component development.
</objective>

<execution_context>
@/Users/yedidya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yedidya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite React TypeScript project</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    tsconfig.node.json
    tsconfig.app.json
    src/main.tsx
    src/App.tsx
    src/vite-env.d.ts
    index.html
    .gitignore
  </files>
  <action>
Run `npm create vite@latest . -- --template react-ts` in the project root directory.

After scaffolding:
1. Update React to v19: `npm install react@^19 react-dom@^19`
2. Install core dependencies: `npm install @supabase/supabase-js @tanstack/react-query zustand`
3. Install Tailwind v4: `npm install tailwindcss @tailwindcss/vite`
4. Install dev dependencies: `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`

Update vite.config.ts to include Tailwind plugin and path aliases:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import path from 'path'

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

Update tsconfig.json and tsconfig.app.json to include path aliases:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Ensure .gitignore includes:
- .env.local
- node_modules
- dist
  </action>
  <verify>
Run `npm install` followed by `npm run build` - should complete without errors.
Run `npx tsc --noEmit` - should have no type errors.
  </verify>
  <done>
Vite project created with React 19, TypeScript strict mode, all dependencies installed, build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS v4 and environment variables</name>
  <files>
    src/styles/globals.css
    .env.example
    .env.local
  </files>
  <action>
Create src/styles/globals.css with Tailwind v4 CSS-based configuration:
```css
@import "tailwindcss";

@theme {
  --color-primary: #10b981;
  --color-primary-dark: #059669;
  --color-background: #0f172a;
  --color-surface: #1e293b;
  --color-text: #f8fafc;
  --color-text-muted: #94a3b8;
}
```

Create .env.example (template for required variables):
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
```

Create .env.local with actual Supabase credentials:
```
VITE_SUPABASE_URL=https://gkagkwpqozymjvehzucy.supabase.co
VITE_SUPABASE_ANON_KEY=<retrieve from Supabase dashboard>
```

Update src/main.tsx to import globals.css:
```typescript
import './styles/globals.css'
```

Update src/App.tsx to use Tailwind classes with dark theme:
```typescript
function App() {
  return (
    <div className="min-h-screen bg-background text-text">
      <h1 className="text-2xl font-bold text-primary p-4">VAT Declaration Manager</h1>
    </div>
  )
}
```
  </action>
  <verify>
Run `npm run dev` and open http://localhost:5173
Verify the page shows "VAT Declaration Manager" with dark background and green accent color.
  </verify>
  <done>
Tailwind v4 configured with dark theme, environment variables set up with .env.example for documentation.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - development server starts on port 5173
2. Open browser to http://localhost:5173 - shows app with dark theme
3. Run `npm run build` - production build completes without errors
4. Run `npx tsc --noEmit` - TypeScript compilation succeeds
5. Verify .env.local exists and contains Supabase URL
6. Verify .env.example documents required variables
</verification>

<success_criteria>
- Vite + React 19 + TypeScript project runs locally
- Tailwind CSS v4 utility classes work (dark theme visible)
- Environment variables configured for Supabase connection
- TypeScript strict mode enabled with path aliases
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
